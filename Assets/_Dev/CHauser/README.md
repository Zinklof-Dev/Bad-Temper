You are assigned as a Musician, Programmer, and Modeler. in that order of importance.

assignments:
IPv4 to hex converter (a function that would take in an IPv4 and convert it to hex, IE 255.255.255 - FFFFFF)
hex to IPv4 converter (a reverse function to turn the hex code previously generated back to IPv4, IE FFFFFF - 255.255.255)

Mock Main Menu, nothing too fancy just enough for a very rough idea, should have an option for both LAN and Online play, with the LAN option opening a rough mock up window to connect via the above HEX code or a direct IPv4.
